---
name: The Way Here
description: 一个会记得你的来路、越聊越懂你的长期朋友
colors:
  archive-teal: "#578a83"
  archive-mist: "#f4f7f6"
  archive-paper: "#fbfcfc"
  archive-ink: "#263440"
  archive-muted: "#5f706f"
  archive-line: "#d9e1df"
  attention-gold: "#b9874c"
typography:
  display:
    fontFamily: "Georgia, Songti SC, STSong, Source Han Serif SC, serif"
    fontSize: "clamp(38px, 5vw, 76px)"
    fontWeight: 500
    lineHeight: 1.08
    letterSpacing: "-0.04em"
  body:
    fontFamily: "Noto Sans SC Variable, PingFang SC, Microsoft YaHei, sans-serif"
    fontSize: "14px"
    fontWeight: 400
    lineHeight: 1.8
rounded:
  control: "5px"
  panel: "8px"
spacing:
  xs: "8px"
  sm: "16px"
  md: "30px"
  lg: "58px"
---

# Design System: The Way Here

## 当前方向

**Creative North Star:「会继续生长的个人档案」**

**Product Mind:「有什么，都可以聊聊」**

界面像一位认识很久、也愿意继续听下去的朋友所保留的个人档案：雾白画布、纸面内容、深蓝灰文字、青绿色路径标记。标题使用中文衬线字传递时间感，正文保持现代无衬线的可读性。结构主要依靠留白、细线和排版形成，不依赖大阴影、渐变和高饱和色块。朋友感来自记得上下文、承认未知和邀请纠正，不来自头像、拟人插画或模拟情绪。

本规范来自 2026-08 最新 UX 方案。首页、原始知识、知识总览与所有知识浏览页遵守同一套鼠尾草档案主题。`archive-foundation.css` 只提供这套主题的共享结构，`archive-theme.css` 负责 token 与视觉规则；页面级样式不得另起品牌色、玻璃材质或大圆角系统。

## 设计原则

- **先读后操作。** 首屏先说明当前页面是什么，再给出少量明确动作。
- **证据是一等信息。** 数量、日期、来源和关联紧邻内容出现，用户能随时回到原文。
- **扁平默认。** 普通集合用边界和相邻色面分层；阴影仅用于真正覆盖页面的浮层。
- **低圆角。** 完整内容面使用 8px，表单和按钮使用 4–5px；胶囊仅用于标签或单一筛选状态。
- **强调色克制。** 青绿色只表达路径、当前状态、焦点和可继续前行的动作；暖金色只表达需要留意的局部状态。
- **内容真实。** 设计稿示例必须接入当前知识库数据，不把示例文案硬编码成产品事实。
- **省略可还原。** 单行省略号和多行截断只负责维持信息密度；文字确实被截断时，鼠标悬停会在统一浮层中显示全文，完整文字不显示多余提示。可聚焦的卡片和控件在键盘聚焦时提供同样反馈；已经拥有专用详情浮层的内容不叠加第二个提示。
- **先问一个具体问题。** 首页和“值得聊聊”不能把空白输入框当作主角，也不能一次铺开问题清单。系统先从可追溯的已有理解与未知中选出一个用户可能关心的问题，直接问；用户可以换一个，也可以主动聊别的。
- **记录入口始终可见。** 首页必须清楚提供“带进生活记录”的入口，并明确支持日记、笔记、AI 对话和消费账单。记录不是冷冰冰的导入任务，而是让后续问题更具体、理解更有来路的另一种起点。
- **一句话只做一件事。** 标题能说明的内容不再用副标题重复，具体问题前不添加“今天我想问你”一类气氛标签，相邻区块不重复同一个动作。两段解释同一件事的小字必须合并或删除。
- **亲近但不越界。** 可以使用“我会记得 / 一起聊聊 / 还不懂你”，同时持续显示证据、未知、实际改动和原始笔记保护，不能用朋友口吻掩盖 Agent 的能力边界。
- **只交付桌面端。** 当前设计与验收范围是桌面 Web；不得在普通功能开发中顺带增加手机、平板、触控或窄屏适配，也不为这些设备制作设计稿或截图。只有产品范围明确调整后才重新设计这些体验。

## 色彩

- `#f4f7f6` 雾白画布：应用背景。
- `#fbfcfc` 档案纸面：内容卡片、阅读区、输入面。
- `#263440` 深蓝灰墨色：标题、正文、主要按钮。
- `#5f706f` 次级灰绿：说明、日期、数量与辅助信息；在纸面与雾白底上保持正文可读对比度。
- `#d9e1df` 档案边界：结构分隔线。
- `#578a83` 路径青绿：活动项、当前节点、链接与焦点。
- `#b9874c` 提醒金：需要停下确认的短标签或状态点。

页面层级优先使用纸面、雾白和细边界。路径青绿和提醒金不得铺满大面积页面容器。

## 字体

展示标题使用 `Georgia / Songti SC / STSong / Source Han Serif SC` 回退栈，字重 500。正文、导航和控件使用本地 `Noto Sans SC Variable`。

- 首页主标题：48–76px，行高 1.14，最多约 12 个汉字宽。
- 页面标题：38–62px，行高 1.08。
- 区块标题：24–40px。
- 正文：13–16px，行高 1.7–1.9。
- 数据标签：10–12px，可使用等宽字体与轻微字距。

衬线字用于标题、引用和关键阅读节点，不用于表单、长段正文或密集文件列表。

## 布局

桌面端全局导航高 64px，知识局部导航高 50px，内容宽度上限 1600px，左右至少保留 32px。一级导航依次为“此刻 / 值得聊聊 / 生活记录 / 已有理解”。首页固定以“有什么，都可以聊聊”作为产品论点，并紧接着直接提出一个可回答的具体问题；不得以空白对话框要求用户自己发明开场，也不得用三卡、九宫格或长列表同时推销多个问题。问题每天稳定轮换，允许“换一个”，完整问题库只通过小型次级入口进入。

首页在首个问题之后必须继续露出一次“带进生活记录”的独立入口，用“日记、对话和账单”说明范围，并以一句话交代它会让后续问题更具体；同一屏内不再出现第二个同名动作。其后仅用单行紧凑索引透出已经形成的理解。首页不得展示“现在的你”、阶段摘要、最近材料、本人引用、知识摘要、补充动作或完整人生轨迹；这些内容属于各自的二级页面。问题不是待办事项；主问题直接以问句出现，不加拟人化提问标签，也不在首屏解释提问理由。

知识总览先显示总量与来源，再进入理解自己、人生轨迹和人与世界三个空间。人生轨迹页面直接以时间线呈现，不设置额外视角切换；近况回信作为独立二级入口，紧邻人生轨迹。原始知识使用文件夹、文件列表、正文阅读三栏结构；目录与文件列表在桌面端保持粘性，页面拥有唯一纵向滚动。

首页、值得聊聊和生活记录统一使用“带一段记录进来”作为导入入口，并保持同一图标与控件样式；首页不再用多个近义按钮重复打开同一流程。来源提供“日记与笔记 / 聊天记录 / 消费账单 / 照片”四个一级类型，各 AI 平台作为聊天记录下的次级选择；弹窗记住上一次选择的类型。所有记录类型统一两步导入：第一步在同一屏完成“选类型 + 加材料”，第二步确认位置。照片沿用相同的进度条和类型卡片，先选择、预览一组照片，再命名并确认位置，随后进入认人物；选图区域建议同一段旅程的一组照片。消费账单也经过同一套两步流程，选择文件时强调它会被整理成“消费旅程”，而非生成财务报表；完成后进入全部候选线索同屏的卡片墙。线索只展示聚类依据与候选关系，不把消费推断写成用户已经确认的经历。

当前桌面设计基线为 1280–1600px 宽的鼠标与键盘环境。新页面和新组件只需维护桌面信息密度与操作路径；不要新增移动端断点、折叠导航、底部抽屉、触控尺寸或单栏重排。仓库中既有的窄屏样式属于历史兼容代码，不构成后续功能的交付要求，也不应在无关任务中扩展。

## 组件约定

### 导航

活动项用字重与底部青绿色细线表达，不使用大面积选中胶囊。个人空间切换、搜索和内容导航各自独立。品牌中的 `Here` 使用路径青绿，作为跨页面稳定签名。

### 按钮

主要动作使用深蓝灰底、白字、5px 圆角；页面内最多突出一个主要动作。次要动作使用透明底或细边界。键盘焦点统一使用半透明青绿色 3px 外轮廓。

### 输入控件

单行文本、多行文本与下拉选择统一复用 `shared/form-controls.tsx` 的 `TextInput`、`TextArea` 和 `SelectInput`。搜索统一使用 `SearchField`，保留原生标签、键盘、表单提交与 ref 行为。

焦点视觉只由 `styles/form-controls.css` 管理：在现有边界内显示青绿色细线，不叠加外部光圈。带图标的搜索与带发送按钮的对话使用 `text-field-shell`，输入聚焦时由外壳显示唯一焦点，内部控件不另画方框；Tab 移到按钮时仍显示按钮自己的键盘轮廓。页面样式只定义尺寸、排版和背景，不再重复定义输入焦点。高对比模式使用内侧系统色轮廓。文件选择、复选框和滑块保留各自原生交互。

### 卡片与列表

首页入口和知识空间使用 8px 纸面卡片；连续材料、人物和知识页面优先使用共享边界的行列表。Hover 只改变纸面色或边界色，不使用明显位移和强阴影。

### 账单记忆

账单记忆使用三列线索卡片墙。顶部同时展示标题、已确认／已简答／已细聊／已跳过／待处理的文字图例、线性进度和唯一主要动作“构建这份记录”。每条线索保持标题、笔数与置信度、候选摘要、状态和操作的稳定结构；处理完成后原位降权，不重排，并可点击结果重新处理。

待处理线索提供“确认为事实 / 简单说说 / 展开细聊 / 暂时不收录”四种明确选择。确认路径必须先原地展开系统拟收录句与账单证据，再由用户确认；成功后提供 8 秒撤销。简答路径固定使用一段开放输入，不生成选项、不追问，空白提交即采用摘要；展开区不重复线索摘要，改为逐笔展示时间、交易对方、商品和金额，最多三行，溢出内容可悬停或聚焦查看完整交易详情。长内容只提示可改用细聊，不阻止保存。“展开细聊”是绑定到单条线索并延续该线索对话历史的入口；关闭记忆弹窗后，生活记录页右下角仍保留原有“聊聊这段旅程”通用入口。

细聊不是为了问齐人物、动机、感受与结果。Agent 只在用户原话留下自然的未完线索、且补充会改变叙述时问一个短问题；允许只回应而不提问，不推断用户未表达的情绪或意义。用户说“不记得”“没什么具体场景”“就是这样”或不想展开时，视为完整答案并停止换角度盘问。对话输入区持续提供“这段先聊到这里”，由用户明确结束；Agent 只确认已经整理的内容，不再抛出问题，界面随后显示完成状态，同时保留“我还想补一句”的可逆入口。

所有线索都获得已确认、已简答、已细聊或已跳过的明确归属后才允许构建，且至少有一条会被摄取。最终确认浮层逐条列出处理结果与跳过项；服务端再次验证门槛，不能从文件菜单或通用构建入口绕过。线索状态与简答原话保存在导入清单，确认内容同步写入报告受管区，刷新或隔日返回不丢失。

### 值得聊聊

“值得聊聊”页一次只把一个问题放在视觉中心，左侧只保留“值得聊聊”页名，不再解释选题机制。问题本身必须是完整、具体、可以直接回答的问句。其下用两个安静区域说明“我现在的理解”和“我还不知道”，主要动作是“说说看”，次要动作是“换一个问题”和“看看依据”。其余问题默认隐藏，只保留一条弱化的“看看其他 N 个问题”入口；只有用户主动展开后才能出现完整列表。无论数据是否充足，都不得用空文本框代替提问，空状态应退回一个关于最近具体小事的问题。

### 阅读与编辑

长文正文在独立阅读页保持舒适行宽，在三栏编辑工作区内自适应占满正文列。标题区、属性区、正文和大纲依次分层；双击进入编辑、自动保存和证据关联能力继续复用同一实现。页面不能出现正文内滚动与浏览器滚动并存的双重视口。

近况回信只有一个版本时保持纯阅读面，不显示版本控件；存在两个及以上版本时，在正文上方显示版本切换器，并默认打开最新完成版本。版本名称使用“雅尼视角回信”一类可读来源标签，同时标明日期与最新版本、历史版本或最初版本；原始回信始终保留。版本区继续使用纸面与雾白的扁平色层、细分隔线、克制圆角和衬线阅读层级；生成版本在正文前显示紧凑的来源元数据行，并提供“查看生成对话”动作。

### 照片记忆

照片导入后进入“认人 / 讲故事”两步纸面弹窗，以照片和用户原话为主体。桌面弹窗宽度上限 1080px，外层圆角 16px，完整照片与故事输入区圆角 10px，人物填写浮卡圆角 8px；这些尺寸仅适用于照片记忆，不改动全局圆角系统。顶部标题 17px，步骤标题 20px；元信息与标签 12px、说明 13px、输入正文 14px。顶部使用两段 3px 细进度线，当前段以墨色标明；两步共享 60px 方形缩略图带，选中照片以边框区分。

认人直接展示完整照片，不设置常驻人物操作区。点击人脸框只进入范围调整并显示边角手柄，整框拖动改位置，四角拖动同时改宽高，上下边中部提供更宽的纵向拖动区，长框无需够到底部角点即可从顶部缩短；点击框上的姓名标签才在框附近浮出人物填写卡，可以关联已有身份或添加称呼。浮卡根据人物框位置自动选择左右和上下方向，内容过长时在卡内滚动，不参与照片或页面布局。已标注人脸使用实线和姓名，待认领人脸使用虚线和“填写人物”；缩略图按人物标注显示“已认完 / 认了一部分”。选择身份后，同组跨照片人脸一起保存，重新打开仍保持分组；误分人脸可以单独拆出标注，“不记录这个人”只移除当前照片中的这一次出现。自动漏检时，用户可进入圈选状态，直接在照片上拖出一个新人物框。点击当前人物框与浮卡之外的位置完整关闭人物表单并取消选中。

讲故事展示整组照片，共用一个故事栏，不再有逐张切换或完成状态点。空故事在标题与说明旁提供带图标的独立实心“AI 帮你写”按钮，说明文案不展示叙述视角要求，一次用所有照片预览，从“我”的视角串成一篇有事实依据的生活故事。生成状态、编辑锁定和核对提示覆盖整组。旧逐张故事与整段讲述合并恢复，保留用户内容。

### 上下文 Agent 抽屉

Agent 只使用一个右下角深蓝灰浮动入口和覆盖页面的右侧抽屉，与知识导航分开，不再设计独立工作台。日常入口称“找我聊聊”，抽屉顶部持续显示当前个人空间与页面标题，内部在新话题、“我们聊过的事”和完整线程详情间切换；普通对话由 Agent 判断只回答还是沉淀，实际变更和验证结果必须清楚展示。人物视角重读也在此抽屉内创建；开始前显示将保留的版本标签，并明确原始回信仍可切换查看，完成后同时在对话详情中确认已保留为最新版本。关闭后将键盘焦点还给实际触发抽屉的控件；若该控件已移除，再回退到固定浮动入口。

运行中的对话将输入框右侧发送按钮替换为实心方块停止按钮，消息区不再放停止链接；按钮位置、尺寸及键盘焦点样式保持一致。停止请求处理中禁用重复操作，失败可重试，结束后恢复发送并保留未发送文字。普通运行仍可在输入框按 Enter 补充说明；验证阶段沿用不可中断的限制。

## 动效与可访问性

仅在首页首屏使用一次轻微上浮淡入。Hover 和焦点转换控制在 180–220ms；尊重 `prefers-reduced-motion`。所有选中状态同时使用形状、文字或位置表达，不能只依赖颜色。

## 视觉验收

- 1280–1600px 桌面宽度无横向溢出，主要操作无需触控或窄屏专用交互。
- 首页、原始知识、知识总览和至少一个知识子页需要截图检查。
- 品牌、导航、标题、边界和操作色在所有页面一致；不得重新引入 Apple 蓝、装饰性玻璃模糊或另一套页面 token。
- 运行 `design:check`、类型检查、前端测试与生产构建。

生活记录页的待完成照片与账单记忆共用顶部缩略卡片，显示类型、标题、数量和当前状态。照片或账单刚导入成功时自动打开一次对应的完整记忆弹窗，让用户直接继续构建；关闭后仍可从顶部卡片再次进入。刷新页面、切换记录或恢复旧任务时不重复自动打开。已构建的记忆退出顶部卡片区域，在下方列表仍可打开查看和调整；账单正文预览顶部不重复放置“打开账单记忆”入口。进入页面、切换记录或恢复已有会话绑定均不自动弹出 Agent；对话只由主动点击开启。记忆弹窗沿用档案纸面、低圆角和固定关闭入口，支持 Escape 与焦点回到原入口；发起对话时交接到 Agent，不叠加两个模态窗口。

照片记忆弹窗的顶部两段进度同时承担步骤导航，支持鼠标和键盘切换；完成标记依实际内容更新，保存或运行时禁用导航。打开时自动接续本机草稿，不显示恢复提示或恢复／丢弃按钮。认人底部提供“跳过这步 / 下一步”，讲故事底部提供“上一步 / 收进理解”；检测不阻塞进入讲故事。未保存的人物修改会在故事页说明原因并阻止收进理解。“收进理解”直接保存并确认讲述、启动构建，不另设核对页或构建步骤。AI 起草、构建进度、失败与完成状态在原位呈现，完成后提供人物图谱入口；只有主动点击“查看进度”才打开 Agent 抽屉。生活记录列表右侧保留 Markdown 正文预览，照片人物及讲述修改仍在弹窗进行。
